import React, { SFC } from "react";

interface IResponseModal {
  bankNotes: Array<object>;
  onClose: Function;
}

const ResponseModal: SFC<IResponseModal> = ({ bankNotes, onClose }): null => {
  return null;
};

export default ResponseModal;
