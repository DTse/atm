import { createStyles, makeStyles, Theme } from "@material-ui/core/styles";

export const useAmountInputStyles = makeStyles((theme: Theme) =>
  createStyles({
    root: {
      padding: "10px 20px"
    }
  })
);
